# Temporal Cloud API - Create Namespace

Create a namespace in Temporal Cloud using the Cloud API.

## Setup

```bash
npm install
```

## Usage

Set your API key and run:

```bash
export TEMPORAL_CLOUD_API_KEY=your-api-key
export NAMESPACE_NAME=my-namespace
export REGION=us-west-2

npm run create-namespace
```
